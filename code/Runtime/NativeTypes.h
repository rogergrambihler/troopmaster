
// platform types but don't want to include winnt.h for example so can abstract these out if port to 
// a different platform.

typedef void *LPVOID;
typedef unsigned long ULONG;
typedef unsigned int UINT;
